<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand"><img class="logo" src="{{ asset('images/logo-no-background.png') }}" alt="Series !">Sortir</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">



                {# Routes StateEventController #}



                {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('event_list') }}">Événements</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('city_list') }}">Villes</a>
                    </li>


                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_list') }}">Utilisateurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('place_list') }}">Lieux</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('place_list') }}">Mes inscriptions</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_logout') }}">Se Déconnecter ({{ app.user.firstName }}) </a>
                    </li>

                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_login') }}">Se connecter </a>
                </li>
                {% endif %}

            </ul>
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Soirée de fin d'année..." aria-label="Search">
                <button class="btn btn-outline-success" type="submit"> Rechercher</button>
            </form>
        </div>
    </div>
</nav>
